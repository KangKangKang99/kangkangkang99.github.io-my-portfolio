/* eslint-disable no-unused-vars */
export enum enumRoutePermission {
    SUPER_ADMIN = 1,
    ADMIN = 2,
    GUEST = 3,
}

export enum enumThemeMode {
    DARK = 'dark',
    LIGHT = ''
}


export enum enumButtonStyle {
    DEFAULT = 1,
    ALTERNATIVE = 2,
    DARK = 3,
    LIGHT = 4, 
    GREEN = 5, 
    RED = 6,
    YELLOW = 7,
    PURPLE = 8
}

export enum enumInputValValid {
    INVALID = 0,
    VALID = 1,
    UNSET =2,
}

export enum enumInputType {
    TEXT = 'text',
    PASSWORD = 'password',
    EMAIL = 'email',
    NUMBER = 'number'

}

export enum enumInputStyle {
    DEFAULT = 1,
    UNDER_LINE =2
}